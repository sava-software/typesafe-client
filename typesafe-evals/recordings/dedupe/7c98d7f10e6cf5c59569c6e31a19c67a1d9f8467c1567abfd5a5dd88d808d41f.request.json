{"state":{"a":{"summary":"`targetTests = \"software.sava.core.crypto.*Tests\"` does not match the plugin-generated `Ripemd160FuzzSeedReplayTest` (singular \"Test\"), so the new corpus replay runs under `check` but contributes nothing as a PIT killer for the crypto suite — unlike the ed25519 suite, whose `*Test*` pattern does include its replays.","failure_scenario":null},"b":{"summary":"The crypto suite's `targetTests = \"software.sava.core.crypto.*Tests\"` does not match the plugin-generated `Ripemd160FuzzSeedReplayTest`, so the committed corpus replay runs under `check` but is not a PIT killer for this suite — unlike the ed25519 suite, whose `*Test*` pattern does include its replay.","failure_scenario":null}},"model":"jev-latest","questions":{"relation":{"type":"score","instructions":{"question":"How does the defect described by `b` relate to the defect described by `a`?","focus":"Compare the defects, not the wording. Two findings about the same code can still be different defects."},"criteria":["Different defects: fixing the defect `a` describes would leave the defect `b` describes unfixed, even when both sit in the same code.","The same underlying defect, but `b` narrows it, re-scopes which instances or inputs it covers, or reports it at a different place. Not a different defect in adjacent code.","The same defect restated in different words: fixing one fixes the other."]},"claims_new_evidence":{"type":"noul","instructions":"Does `b` cite evidence that `a` does not?","criteria":{"true":"`b` names a file, test, command, observation, or reproduction that `a` does not mention.","false":"`b` cites nothing beyond what `a` already cites, or cites nothing at all."}}}}