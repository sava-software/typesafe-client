{"state":{"a":{"summary":"A newly documented non-guessable behaviour — createTx reordering the caller's AccountMeta[] in place — was not added to the file whose stated purpose is to make that family visible in one place.","failure_scenario":"CONVENTIONS.md's \"Byte-array ownership\" section already covers the neighbouring hazard (`PublicKey.createPubKey(bytes)` retaining its input, callers passing `bytes.clone()`), so a consumer auditing 25.11.0 for array-ownership surprises reads that section, sees only byte[] retention, and misses that two public createTx overloads permute an AccountMeta[] they were handed. The repo's own contract says these behaviours are \"documented at the declaration too; this file exists so the family is visible in one place\" — the declaration half was done in this delta and the index half was not, so the file is now silently incomplete for exactly the release that introduces the documentation."},"b":{"summary":"A newly documented caller-visible array-ownership surprise was added at the declaration but not to CONVENTIONS.md, the file whose stated purpose is to make that family visible in one place.","failure_scenario":"A consumer follows the file's own instruction (\"Read this before writing assertions against an API you have not used here\") and reads the `## Byte-array ownership` section. It tells them which factories retain an input array and that they can pass `bytes.clone()`. It says nothing about a factory that reorders an input array. They call the four-arg `createTx` with a reused `sortedAccounts`, reuse it afterward, and get the wrong account order — the exact behavior the delta just documented and that uses the same `clone()` remedy the section already teaches."},"same_file":true,"line_delta":0,"cosine":0.32116788321167883},"model":"jev-latest","questions":{"relation":{"type":"score","instructions":{"question":"How does the defect described by `b` relate to the defect described by `a`?","focus":"Compare the defects, not the wording. Two findings about the same code can still be different defects."},"criteria":["Different defects: fixing the defect `a` describes would leave the defect `b` describes unfixed, even when both sit in the same code.","The same underlying defect, but `b` narrows it, re-scopes which instances or inputs it covers, or reports it at a different place. Not a different defect in adjacent code.","The same defect restated in different words: fixing one fixes the other."]},"claims_new_evidence":{"type":"noul","instructions":"Does `b` cite evidence that `a` does not?","criteria":{"true":"`b` names a file, test, command, observation, or reproduction that `a` does not mention.","false":"`b` cites nothing beyond what `a` already cites, or cites nothing at all."}}}}