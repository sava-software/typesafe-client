{"state":{"a":{"summary":"The single-table overload performs the identical in-place compaction of the caller's array but is undocumented, and the newly documented overload delegates to it for exactly one lookup table.","failure_scenario":"A consumer reads the new warning on the LookupTableAccountMeta[] overload, concludes the sibling is safe, and writes:\n\n  var sorted = TransactionRecord.sortV0Accounts(merged);   // canonical v0 order\n  var v0 = Transaction.createTx(ix, len, sorted, table);    // silently permutes `sorted`\n  var legacyTwin = Transaction.createTx(ix, len, sorted);   // now non-canonical\n\nWith sorted = {payer(signer,write), writeY(write, not in table), writeX(write, in table), program(invoked, readonly)}, the first call leaves sorted = {payer, writeY, program, writeX}. The legacy build at line 176 writes accounts in array order and derives the header from flags: numRequiredSignatures=1, numReadonlySignedAccounts=0, numReadonlyUnsignedAccounts=1. The runtime derives writability from position, so indices 1..2 (writeY and `program`) are treated as writable and index 3 (writeX) as readonly — the program id is granted write access and the account the instruction actually needs to write is readonly. The transaction is rejected or executes with the wrong permissions. The same permutation also breaks any Arrays.binarySearch/parallel-index structure the caller kept over `sorted`."},"b":{"summary":"The new sentence \"With no lookup tables, rebuilds from `instructions`\" invites passing `null` for `tableAccountMetas` - which every sibling overload accepts as \"no tables\" - but this overload dereferences it unguarded and throws NullPointerException.","failure_scenario":"`Transaction.createTx(instructions, len, sortedAccounts, (LookupTableAccountMeta[]) null)` throws NullPointerException at Transaction.java:443, whereas the adjacent `Transaction.createTx(instructions, len, mergedAccountsMap, (LookupTableAccountMeta[]) null)` returns a legacy transaction. A consumer migrating from the Map overload to the array overload - the migration the new javadoc is written to support - gets a crash where the doc promised a legacy rebuild."}},"model":"jev-latest","questions":{"relation":{"type":"score","instructions":{"question":"How does the defect described by `b` relate to the defect described by `a`?","focus":"Compare the defects, not the wording. Two findings about the same code can still be different defects."},"criteria":["Different defects: fixing the defect `a` describes would leave the defect `b` describes unfixed, even when both sit in the same code.","The same underlying defect, but `b` narrows it, re-scopes which instances or inputs it covers, or reports it at a different place. Not a different defect in adjacent code.","The same defect restated in different words: fixing one fixes the other."]},"claims_new_evidence":{"type":"noul","instructions":"Does `b` cite evidence that `a` does not?","criteria":{"true":"`b` names a file, test, command, observation, or reproduction that `a` does not mention.","false":"`b` cites nothing beyond what `a` already cites, or cites nothing at all."}}}}