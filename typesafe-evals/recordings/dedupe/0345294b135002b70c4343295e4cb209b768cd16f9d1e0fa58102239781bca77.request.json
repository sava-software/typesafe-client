{"state":{"a":{"summary":"A newly documented non-guessable behaviour — createTx reordering the caller's AccountMeta[] in place — was not added to the file whose stated purpose is to make that family visible in one place.","failure_scenario":"CONVENTIONS.md's \"Byte-array ownership\" section already covers the neighbouring hazard (`PublicKey.createPubKey(bytes)` retaining its input, callers passing `bytes.clone()`), so a consumer auditing 25.11.0 for array-ownership surprises reads that section, sees only byte[] retention, and misses that two public createTx overloads permute an AccountMeta[] they were handed. The repo's own contract says these behaviours are \"documented at the declaration too; this file exists so the family is visible in one place\" — the declaration half was done in this delta and the index half was not, so the file is now silently incomplete for exactly the release that introduces the documentation."},"b":{"summary":"CONVENTIONS.md's \"Byte-array ownership\" section is the repo's stated one-place index for exactly this caller-array-ownership family, and the delta added a new member of it without indexing it there.","failure_scenario":"A contributor follows CONVENTIONS.md's own promise at lines 8-9 (\"Where a convention is surprising it is documented at the declaration too; this file exists so the family is visible in one place\") to learn which sava APIs take ownership of a caller's array. They find `PublicKey.createPubKey`, `Discriminator.createDiscriminator` and `toByteArray`, conclude the family is complete, and write a builder that reuses one `AccountMeta[]` across several `createTx` calls - the exact hazard the new javadoc was written to prevent, and the exact remedy (\"pass a clone\") that section already teaches for the byte-array cases."},"same_file":true,"line_delta":0,"cosine":0.3308270676691729},"model":"jev-latest","questions":{"relation":{"type":"score","instructions":{"question":"How does the defect described by `b` relate to the defect described by `a`?","focus":"Compare the defects, not the wording. Two findings about the same code can still be different defects."},"criteria":["Different defects: fixing the defect `a` describes would leave the defect `b` describes unfixed, even when both sit in the same code.","The same underlying defect, but `b` narrows it, re-scopes which instances or inputs it covers, or reports it at a different place. Not a different defect in adjacent code.","The same defect restated in different words: fixing one fixes the other."]},"claims_new_evidence":{"type":"noul","instructions":"Does `b` cite evidence that `a` does not?","criteria":{"true":"`b` names a file, test, command, observation, or reproduction that `a` does not mention.","false":"`b` cites nothing beyond what `a` already cites, or cites nothing at all."}}}}