{"state":{"a":{"summary":"The newly documented caller-array mutation was not added to CONVENTIONS.md, which the repo declares to be the one place this family is visible and which already carries a \"Byte-array ownership\" section for exactly this kind of retained/shared-array surprise.","failure_scenario":"A contributor follows CONVENTIONS.md's own instruction — \"Read this before writing assertions against an API you have not used here\" — before calling `Transaction.createTx(ixs, len, sortedAccounts, tables)`. The file lists `PublicKey.createPubKey` retention and `toByteArray()` sharing but says nothing about `createTx` reordering the array they passed, so they reuse the array and hit the header/privilege corruption described in finding 1. CONVENTIONS.md:8-9 states the contract this misses: \"Where a convention is surprising it is documented at the declaration too; this file exists so the family is visible in one place.\""},"b":{"summary":"CONVENTIONS.md's array-ownership family was not extended with the newly documented caller-array mutation, so the file's stated guarantee that the family is 'visible in one place' no longer holds for the behaviour this delta just established as a published contract.","failure_scenario":"A consumer following the repo's own stated navigation reads CONVENTIONS.md's 'Byte-array ownership' section to learn which sava APIs take ownership of caller-supplied arrays. It tells them createPubKey/createDiscriminator retain their input and that they can pass bytes.clone(). It says nothing about Transaction.createTx permuting a caller-supplied AccountMeta[] in place. They conclude no other array-ownership hazard exists, pass their own sorted array to createTx, reuse it, and get the permutation described in the finding above. The file's premise — that it exists so the family is visible in one place — is what makes the omission actively misleading rather than merely incomplete."}},"model":"jev-latest","questions":{"relation":{"type":"score","instructions":{"question":"How does the defect described by `b` relate to the defect described by `a`?","focus":"Compare the defects, not the wording. Two findings about the same code can still be different defects."},"criteria":["Different defects: fixing the defect `a` describes would leave the defect `b` describes unfixed, even when both sit in the same code.","The same underlying defect, but `b` narrows it, re-scopes which instances or inputs it covers, or reports it at a different place. Not a different defect in adjacent code.","The same defect restated in different words: fixing one fixes the other."]},"claims_new_evidence":{"type":"noul","instructions":"Does `b` cite evidence that `a` does not?","criteria":{"true":"`b` names a file, test, command, observation, or reproduction that `a` does not mention.","false":"`b` cites nothing beyond what `a` already cites, or cites nothing at all."}}}}