# Parity findings, lens: response

Each entry: verdict from an independent refuter, then the finder's claim and the refuter's evidence. Treat the corrected proposal as the spec.


## response-1. [high, wire-mismatch] CONFIRMED: Score legend values are parsed as strings, but the wire echoes back object/array criteria

- Java: `/Users/jim/src/typesafe-client/typesafe-client/src/main/java/software/sava/typesafe/Answer.java:89-96`
- Reference: `/Users/jim/docs/ai/typesafe/typesafe-sdk-python/src/typesafe_sdk/_core/response_types.py:55 (legend: dict[int, str | dict[str, Any] | list[Any]]); generated wire schema /Users/jim/docs/ai/typesafe/typesafe-sdk-python/src/typesafe_sdk/_schemas/models.py:121; pinned by /Users/jim/docs/ai/typesafe/typesafe-sdk-python/tests/test_responses.py:178-201; JS ScoreLegend<T> = { [score in ScoreOf<T>]: T[score] } at /Users/jim/docs/ai/typesafe/typesafe-sdk-js/src/types.ts:98-100 over ScoreCriteria = readonly [EntryType, ...] at types.ts:49`
- Java does: Answer.Parser case 6 builds a LinkedHashMap<String,String> with `ji1.readString()` per legend value. jsoniter's readString (BaseJsonIterator.java:205-215) accepts only a string or null and throws on `{` or `[`, so the whole answers object fails to parse and TypeSafeClientImpl.java:31-33 wraps it in TypeSafeParseException.
- Reference does: Both SDKs type the legend value as the echoed criterion entry, which is explicitly text, a JSON object, an array, or null. Python's test_response_preserves_nested_json round-trips `legend={0: {"examples": ["a", {"note": None}]}}` and asserts the nested list and dict survive.
- Consequence: This is reachable from the Java client itself: Score.java:14 takes `List<JsonContent> levels`, so a caller who passes `JsonContent.object()...` or `JsonContent.array(...)` levels (the documented rich-description form, exercised live in /Users/jim/docs/ai/typesafe/typesafe-sdk-js/test/integration/api.integration.ts:87-99 for noul/choice) gets the criteria echoed into `legend` and then every single response for that request throws TypeSafeParseException. All other answers in the same response are lost with it.
- Refuter's evidence: Answer.java:89-96 builds `LinkedHashMap<String,String>` with `ji1.readString()` per legend value; BaseJsonIterator readString is `final char c = nextToken(); if (c == '"') parseString(); else if (c == 'n') {skipNull(); return null;} else throw reportError("readString", "expected string or null, but " + c)` — so `{` or `[` throws, and the throw propagates out of SystemOneResponse.parse into TypeSafeClientImpl.java:31-33 (`catch (final RuntimeException parseCause) { throw parseFailure(...) }`), losing the whole response. ScoreAnswer.java:10 types the field `Map<Integer, String>`. The wire contract does allow non-string legend values: the codegen'd schema at typesafe-sdk-python/src/typesafe_sdk/_schemas/models.py:121 is `legend: dict[str, str | dict[str, Any] | list[Any]] = Field(...)` described as "The requested criteria mapped to their score levels"; the hand-written model at _core/response_types.py:55 widens the key only. JS types.ts:98-100 `ScoreLegend<T> = { readonly [score in ScoreOf<T>]: T[score] }` over `ScoreCriteria = readonly [EntryType, EntryType, ...EntryType[]]` (types.ts:49) with `EntryType = string | {…} | JsonValue[] | null` (types.ts:11) — the legend value is literally the echoed criterion entry. Java Score.java:14 accepts `List<JsonContent> levels`, so the Java client can send object/array levels. Two citation nits that do not change the verdict: test_responses.py:177-201 (`test_response_preserves_nested_json`) constructs a `ScoreAnswer` in Python and `model_dump()`s it — it is a model round-trip, not an HTTP decode; and api.integration.ts:87-99 exercises rich descriptions for noul and choice only, not for score, so it is not live evidence of an object legend coming back.
- **Corrected proposal:** Legend values must be parsed as JSON values, not strings. Change ScoreAnswer.legend to `Map<Integer, JsonContent>` (or capture each value's raw text via a skip-and-slice) so an object, array or null level survives; today Answer.java:92 `ji1.readString()` throws on `{`/`[` and TypeSafeClientImpl.java:31-33 turns the entire response — every other answer included — into a TypeSafeParseException. Pin an AnswerParseTests case on `{"type":"score","score":0.0,"confidence":1.0,"legend":{"0":{"examples":["a",{"note":null}]},"1":"ok"},"probabilities":{"0":0.5,"1":0.5}}`, shaped after the legend in test_responses.py:177-201 and permitted by _schemas/models.py:121. Note `readString` already returns null for an echoed `null` level, so a null level currently parses to a null map value rather than throwing — cover that too. Do not cite api.integration.ts:87-99 as evidence: it covers noul/choice rich criteria, not score.


## response-2. [high, missing-validation] CONFIRMED: Missing required answer fields silently default to 0.0 / null instead of failing

- Java: `/Users/jim/src/typesafe-client/typesafe-client/src/main/java/software/sava/typesafe/Answer.java:34-40 and 45-58`
- Reference: `/Users/jim/docs/ai/typesafe/typesafe-sdk-python/tests/test_responses.py:33-36; required-field declarations in /Users/jim/docs/ai/typesafe/typesafe-sdk-python/src/typesafe_sdk/_schemas/models.py:13-30 (choice, confidence, probabilities), :75-80 (noul), :109-132 (score, confidence, legend, probabilities)`
- Java does: `noul`, `confidence` and `score` are primitive `double` fields left at 0.0 when the field is absent, and `choice` is left null. `create()` checks only that `type` is non-null and constructs the record from whatever was set.
- Reference does: Python raises TypeSafeAPIResponseValidationError with field_path `answers.n.noul`, `answers.c.confidence`, `answers.c.choice` for exactly these bodies; every one of those fields is `Field(...)` (required) in the generated wire schema.
- Consequence: A truncated or partially-serialized answer becomes a valid-looking object with the most dangerous possible values: NoulAnswer(0.0) reads as "definitely no", ChoiceAnswer confidence 0.0 reads as "maximum uncertainty", ScoreAnswer score 0.0 reads as the bottom rubric level, and a null `choice` NPEs later at the caller's `switch`/`equals` rather than at the parse boundary. Since the whole API surface is thresholds on these numbers, code silently takes the wrong branch instead of erroring.
- Refuter's evidence: Answer.java:34-40 declares `private double noul; private String choice; private double confidence; private double score;` — primitives initialised to 0.0 and a null String — and create() at Answer.java:45-58 checks only `if (type == null) throw new IllegalStateException("answer without a type")` before building the record from whatever the field predicate happened to set. ChoiceAnswer.java:9 and NoulAnswer.java:4 are plain records with no compact constructor and no requireNonNull, so `new ChoiceAnswer(null, Map.of(), 0.0)` is constructed without complaint. Every one of those fields is required in the wire schema: _schemas/models.py:13 `choice: str = Field(...)`, :19 `confidence: float = Field(...)`, :25 `probabilities`, :75 `noul: float = Field(...)`, :110 `score`, :115 `confidence`, :121 `legend`, :127 `probabilities`. Python's parametrised table at tests/test_responses.py:33-36 pins exactly the four bodies: `{"n": {"type": "noul"}}` → field_path `answers.n.noul`, `{"c": {"type": "choice", "choice": "a", "probabilities": {}}}` → `answers.c.confidence`, `{"c": {"type": "choice", "confidence": 0.5, "probabilities": {}}}` → `answers.c.choice`, each asserted to raise TypeSafeAPIResponseValidationError with status 200 and request_id. The existing Java test `missingTypeIsAnError` (AnswerParseTests.java:86-88) shows the only absence currently detected is `type`.
- **Corrected proposal:** Track a seen-field bitmask in Answer.Parser and have create() (Answer.java:45-58) throw when a field the resolved type requires is absent — `noul` for noul, `choice`/`confidence` for choice, `score`/`confidence` for score — so TypeSafeClientImpl.java:31-33 surfaces it as a TypeSafeParseException at the boundary instead of handing back NoulAnswer(0.0) ("definitely no"), ChoiceAnswer(null, …, 0.0) or ScoreAnswer(0.0, …) (bottom rubric level). Pin the three Python bodies from test_responses.py:33,35,36 as AnswerParseTests cases. Keep the existing deliberate leniency for absent `probabilities`/`legend` maps (pinned by `absentMapsParseAsEmpty`, AnswerParseTests.java:137-147) or decide it explicitly — Python treats those as required too (_schemas/models.py:25,121,127).


## response-3. [medium, missing-validation] PARTLY: A 200 body that is not a System One response parses into an empty response with a null model

- Java: `/Users/jim/src/typesafe-client/typesafe-client/src/main/java/software/sava/typesafe/SystemOneResponse.java:42-48; behaviour pinned at /Users/jim/src/typesafe-client/typesafe-client/src/test/java/software/sava/typesafe/AnswerParseTests.java:129-135`
- Reference: `/Users/jim/docs/ai/typesafe/typesafe-sdk-python/tests/test_responses.py:32 (`({}, "model")`) with /Users/jim/docs/ai/typesafe/typesafe-sdk-python/src/typesafe_sdk/_core/response_types.py:105-109 and _schemas/models.py:219-235 (model, answers, usage all required; answers min_length=1)`
- Java does: Parser leaves `model` null and `answers` null; parse() substitutes `Map.of()` for the null answers and returns a SystemOneResponse. `{}`, `null`, `{"detail":"..."}`, or any other JSON object served with a 200 all succeed.
- Reference does: Python fails the decode with field_path `model` and status 200, carrying the request id and the body, so the caller sees the bad response at the boundary.
- Consequence: A proxy, a cached error page served as JSON, or an API shape change returns a 200 that the Java client reports as success. The caller only discovers it at `response.answer(id)`, which throws NoSuchElementException with a useless "have []" message and no status, request id, or body to file a support ticket with — and code that iterates `answers()` instead just does nothing.
- Refuter's evidence: The model half is confirmed. SystemOneResponse.java:42-48 returns `new SystemOneResponse(parser.model, parser.answers == null ? Map.of() : …, parser.usage, requestId, raw)` with no check on `parser.model`, and BaseJsonIterator.testObject returns cleanly on `{`+`}` and on `n`/skipNull, so `{}`, a bare `null` and `{"detail":"…"}` (unknown field → `default -> ji.skip()` at :107) all succeed with model null. The caller then hits SystemOneResponse.java:57 `throw new NoSuchElementException("no answer for question '" + id + "'; have " + answers.keySet())` — no status, request id or body. Python fails this at the boundary: _core/response_types.py:105 `model: str` is required and tests/test_responses.py:32 pins `({}, "model")` with status 200 and request_id req-123. The answers half is refuted. The runtime model at _core/response_types.py:109 is `answers: dict[str, Answer] = Field(default_factory=dict)` — it deliberately overrides the generated `Field(..., min_length=1)` at _schemas/models.py:226-232, and Python's own mocked handlers return `{"model": "jev-latest", "usage": {}, "answers": {}}` (tests/test_types.py:66,80,97,131), so an empty/absent answers map is accepted by the reference client, matching the Java client's deliberate pin at AnswerParseTests.java:129-135. That test also does not pin a null model: its body is `{"model":"m"}`, so it pins only answers-absent → Map.of() and usage-absent → null.
- **Corrected proposal:** Fail the parse when `model` is absent, matching _core/response_types.py:105 and the `({}, "model")` case at test_responses.py:32, and add a wire test asserting TypeSafeParseException for a 200 `{"detail":"…"}` body so a proxy or cached error page served as JSON is not reported as success. Drop the `answers` part of the proposal: the reference client explicitly defaults answers to `{}` (_core/response_types.py:109, overriding the generated min_length=1 at _schemas/models.py:226-232) and its own tests serve `"answers": {}`, so Java's empty-answers tolerance — pinned at AnswerParseTests.java:129-135 — matches. Correct the citation: :129-135 pins answers-absent and usage-absent for a body that does carry a model, not a null model.


## response-4. [medium, missing-validation] CONFIRMED: A malformed /v1/models envelope is reported as an empty model list, not an error

- Java: `/Users/jim/src/typesafe-client/typesafe-client/src/main/java/software/sava/typesafe/ModelCard.java:27 (`holder.models == null ? List.of() : holder.models`); pinned as intended at /Users/jim/src/typesafe-client/typesafe-client/src/test/java/software/sava/typesafe/AnswerParseTests.java:165`
- Reference: `/Users/jim/docs/ai/typesafe/typesafe-sdk-js/src/resources/models.ts:23-28 and /Users/jim/docs/ai/typesafe/typesafe-sdk-js/test/client.test.ts:176-185; /Users/jim/docs/ai/typesafe/typesafe-sdk-python/src/typesafe_sdk/_core/response_types.py:156-166 with tests/test_responses.py:61-68`
- Java does: Returns `List.of()` for `{}` and for `{"ok":true}`; jsoniter's readList treats a JSON `null` as an empty array (JsonIterator.java:209-213), so `{"models":null}` and a bare `null` body also return an empty list. Only `{"models":"bad"}` throws.
- Reference does: JS rejects all of `null`, `[]`, `{"models":{"models":[]}}`, `{"models":null}`, `{"models":"bad"}`, `{"ok":true}` with "Unexpected response shape from GET /v1/models". Python's `models` field is required and a missing per-card field raises with field_path `models[1].name`.
- Consequence: An account that actually has models looks like it has none. Code that picks a model from `models()` (e.g. resolving an alias, or a startup health check) silently degrades to "no models available" instead of surfacing a bad response, and there is no status/request-id-bearing exception to log.
- Refuter's evidence: ModelCard.java:27 `return holder.models == null ? List.of() : holder.models;` — `holder.models` is only assigned inside `if (fieldIndex == 0)`, so `{}` and `{"ok":true}` (which take the `ji1.skip()` branch at :23) return an empty list; AnswerParseTests.java:165 pins `assertEquals(List.of(), ModelCard.parseList(JsonIterator.parse("{}")))` as intended behaviour. For `{"models":null}`, readList → readCollection → readArray, and BaseJsonIterator.readArray has `else if (c == 'n') { skipNull(); return false; }`, so the list comes back empty and non-null; JsonIterator.java:209-211 documents "A JSON `null` value reads as an empty list". A bare `null` body is swallowed by testObject's `else if (c == 'n') { skipNull(); return; }`. Only `{"models":"bad"}` hits readArray's final `throw reportError("readArray", "expected [ or , or n or ], but found: " + c)`. JS rejects all six shapes: resources/models.ts:23-27 `if (Array.isArray(wire?.models)) return wire.models; throw new TypeSafeError("Unexpected response shape from GET /v1/models; expected { models: [...] }.")`, pinned over `[null, [], {models:{models:[]}}, {models:null}, {models:"bad"}, {ok:true}]` at test/client.test.ts:177-185. Python's `models: tuple[ModelMetadata, ...]` is required at _core/response_types.py:156, and a missing per-card field raises with field_path `models[1].name` (tests/test_responses.py:61-68).
- **Corrected proposal:** Distinguish "envelope carried no `models` key" from "`models` was an empty array" in ModelCard.parseList (ModelCard.java:15-27) and throw on the former, so TypeSafeClientImpl.java:36-43 turns it into a TypeSafeParseException carrying status, request id and body instead of an empty list that reads as "this account has no models". Also reject an explicit `{"models":null}` and a bare `null` body, which readArray currently swallows into an empty list (BaseJsonIterator.readArray, `c == 'n'` branch). Replace the `{}` pin at AnswerParseTests.java:165 and add a parametrized test over the JS shape matrix at test/client.test.ts:177-182.


## response-5. [medium, behaviour-gap] PARTLY: Usage conflates "not reported" with zero, and would throw outright on a null token count

- Java: `/Users/jim/src/typesafe-client/typesafe-client/src/main/java/software/sava/typesafe/Usage.java:8 and :26-27`
- Reference: `/Users/jim/docs/ai/typesafe/typesafe-sdk-python/src/typesafe_sdk/_core/response_types.py:65-73 (`input_tokens: int | None = None` — "or `None` when the API did not report it"); observed wire form `"usage": {}` in /Users/jim/docs/ai/typesafe/typesafe-sdk-python/tests/test_types.py:66,80,97,131; live guard `result.usage.input_tokens is None or ...` in /Users/jim/docs/ai/typesafe/typesafe-sdk-python/tests/test_integration.py:40-41`
- Java does: `long inputTokens` / `long outputTokens` default to 0 when the field is absent, so `"usage":{}` becomes `Usage(0, 0)` indistinguishable from a genuinely free request. If the API ever sends `"input_tokens": null`, jsoniter's readLong (BaseJsonIterator.java:1381-1400, peekIntDigitChar path) throws "expected 0~9" and the entire response is lost to a TypeSafeParseException.
- Reference does: Python deliberately widens both counts to `int | None` against the generated schema's required `int`, and its own live integration test asserts the None case is expected in production. The SDK's mocked handlers routinely return `"usage": {}`.
- Consequence: Java callers metering spend against `usage().inputTokens()` under-report silently (the user's stated $5/month budget is exactly this use), and cannot tell a zero-token response from an unreported one. The null variant, which Python's typing anticipates, would fail every affected call.
- Refuter's evidence: The mechanism is confirmed. Usage.java:8 `public record Usage(long inputTokens, long outputTokens)` with `private long inputTokens; private long outputTokens;` defaulting to 0 and Usage.java:26-27 `case 0 -> inputTokens = ji.readLong(); case 1 -> outputTokens = ji.readLong();` — so `"usage":{}` yields `Usage(0, 0)`, indistinguishable from a genuinely free request. And on an explicit JSON null, BaseJsonIterator.readLong() takes `nextToken()` = 'n', which is neither '"' nor '-', falls to `readLong(c)`, and `intDigit('n')` is INVALID_CHAR_FOR_NUMBER → `throw reportError("readLong", "expected 0~9")`, losing the whole response via TypeSafeClientImpl.java:31-33. Two overstatements. First, the framing "observed wire form" for tests/test_types.py:66,80,97,131 is wrong: those are the SDK's own mocked handler bodies (`httpx2.Response(200, json={"model": "jev-latest", "usage": {}, "answers": {}})`), not captured traffic. Second, Python's `input_tokens: int | None = None` (_core/response_types.py:70-73, "or `None` when the API did not report it") and the live guard at tests/test_integration.py:40-41 evidence the *absent key* case, which is exactly `"usage": {}`; an explicit `"input_tokens": null` on the wire is inferred from pydantic's tolerance, not shown anywhere. Also, Java does distinguish a wholly absent `usage` object: SystemOneResponse.java:106 leaves `usage` null, pinned at AnswerParseTests.java:133 — the conflation is only within a present-but-partial usage object.
- **Corrected proposal:** Make both token counts nullable (`Long`, or an OptionalLong accessor) so `"usage":{}` is distinguishable from a genuinely zero-token response, matching _core/response_types.py:70-73 where the reference client deliberately widens the generated required `int` to `int | None` for exactly this case, and its live test tolerates it (tests/test_integration.py:40-41). Read each count tolerantly — only call `ji.readLong()` when the next token is a number — so an explicit `"input_tokens": null` does not throw "expected 0~9" and take the whole response with it (Usage.java:26-27, BaseJsonIterator.readLong). Pin `{"model":"m","usage":{},"answers":{}}` and `{"usage":{"input_tokens":null,"output_tokens":3}}`. Note that an entirely absent `usage` object already yields a null Usage (SystemOneResponse.java:106, pinned at AnswerParseTests.java:133), so only the partial-object case is ambiguous; and cite test_types.py:66 as an SDK fixture, not as observed wire.


## response-6. [medium, fixture] CONFIRMED: No response fixtures from either SDK are pinned in the Java tests

- Java: `/Users/jim/src/typesafe-client/typesafe-client/src/test/java/software/sava/typesafe/TestBodies.java:9-13 (only one live smoke body and one models body)`
- Reference: `/Users/jim/docs/ai/typesafe/typesafe-sdk-python/tests/test_clients.py:42-56 (RESULT, CARD); /Users/jim/docs/ai/typesafe/typesafe-sdk-python/tests/test_responses.py:140-175 (extra fields, unknown answer type); /Users/jim/docs/ai/typesafe/typesafe-sdk-js/test/client.test.ts:29-33 (SYSTEM_ONE_RESPONSE) and :186-195 (extra model card fields)`
- Java does: TestBodies holds one captured live response and one models list. None of the SDKs' shared fixtures, which both reference clients assert against in dozens of tests, are represented.
- Reference does: Both SDKs drive nearly every response test off a small set of canonical bodies, which is what makes their behaviour comparable across languages.
- Consequence: The Java client has no cross-checked corpus: any of the divergences above could be introduced or re-introduced without a test noticing, and there is nothing to diff against when an SDK bumps its fixture after an API change.
- Refuter's evidence: TestBodies.java:9-13 holds exactly two bodies — SMOKE (line 9-10, the 2026-09-17 live call) and MODELS (line 12-13). The only other stored bodies in the Java test tree are three fuzz seeds under src/test/resources/fuzz/response/ (smoke-2026-09-17.bin, docs-choice.bin, unknown-type.bin), and SystemOneResponseFuzz.java:12-18 asserts nothing about their content beyond `catch (final RuntimeException expected)` — they are mutator seeds, not cross-checked fixtures. None of the SDKs' canonical bodies appear: test_clients.py:42-56 RESULT (all three answer types in one response) and :57 CARD, test_responses.py:141-144 (extra `reasoning_tokens`/`billing_units`/`explanation`), :157-165 (unknown answer type), :177-201 (nested-JSON legend), test_types.py:66 (`"usage": {}`), client.test.ts:29-33 SYSTEM_ONE_RESPONSE and :187-195 (model card with an unmodelled `tags` field) — the bodies both reference clients drive nearly every response test off.
- **Corrected proposal:** Add the SDK bodies verbatim to TestBodies and assert the same field values the SDKs do: RESULT (test_clients.py:42-56), the extra-unknown-fields body (test_responses.py:141-144), the unknown-answer-type body (test_responses.py:157-165), `{"model":"jev-latest","usage":{},"answers":{}}` (test_types.py:66), the nested-JSON legend (test_responses.py:177-201), CARD with `release_date: "2026-08-01"` (test_clients.py:57 — not :56), and the model card carrying `tags: ["internal"]` (client.test.ts:187-195). Reuse them as fuzz seeds too: src/test/resources/fuzz/response/ already carries three seeds, but SystemOneResponseFuzz.java:12-18 only asserts "RuntimeException or nothing", so they pin no field values.


## response-7. [low, missing-parse] CONFIRMED: UnknownAnswer keeps only the type string and discards the payload

- Java: `/Users/jim/src/typesafe-client/typesafe-client/src/main/java/software/sava/typesafe/UnknownAnswer.java:5; produced at Answer.java:57`
- Reference: `/Users/jim/docs/ai/typesafe/typesafe-sdk-python/src/typesafe_sdk/_core/response_types.py:89-92 with tests/test_responses.py:157-175; JS keeps the parsed object as-is (no runtime narrowing) at /Users/jim/docs/ai/typesafe/typesafe-sdk-js/src/client.ts:342-346`
- Java does: Every field of an unrecognized answer type is skipped; the record carries the type name only. The payload is recoverable only by re-parsing `SystemOneResponse.raw()` by hand.
- Reference does: Python drops the answer from `answers` but logs a warning and documents that the payload stays reachable via `raw_http_response`; JS never narrows at runtime, so a caller reading `answers.mystery.value` gets the real value (test_responses.py:175 asserts the raw body still carries `{"type":"aurora","value":3}`).
- Consequence: A Java caller who wants to opportunistically use a new answer type before the client models it has to re-parse the raw body themselves, and gets no signal (no log, per the deliberate no-logging choice) that an answer was dropped.
- Refuter's evidence: UnknownAnswer.java:5 `public record UnknownAnswer(String type) implements Answer {}` — the type name is the whole record. Answer.java:57 `default -> new UnknownAnswer(type);`, and every field the FieldMatcher does not name reaches Answer.java:97 `default -> ji.skip()`, so the payload is gone once parsing returns; AnswerParseTests.java:79-83 pins only `assertEquals("rank", unknown.type())`. Recovering the body means re-parsing SystemOneResponse.raw() (SystemOneResponse.java:27,36) by hand. Python drops the answer but warns and documents the escape hatch: _core/response_types.py:89-93 `# Forward-compat: ignore answer types this SDK version does not model. The raw payload is still available through 'raw_http_response'.` + `logger.warning(...)` + `del answers[name]`, with tests/test_responses.py:173-175 asserting `set(result.answers) == {"spam"}` and `result.raw_http_response.json()["answers"]["mystery"]["type"] == "aurora"`. JS never narrows at runtime — client.ts:342-346 `const parsed = await parseBody(res); … return parsed as T;` — so `answers.mystery.value` returns the real value.
- **Corrected proposal:** Carry the unknown answer's body text on UnknownAnswer (a raw JSON slice captured instead of `ji.skip()` at Answer.java:97), so a caller can opportunistically read a new answer type without re-parsing SystemOneResponse.raw() and without any signal that an answer was dropped (the no-logging choice means Python's `logger.warning` at _core/response_types.py:92 has no Java counterpart). Add a test over the body at test_responses.py:157-165 asserting the `mystery` payload survives alongside the parsed `spam` noul. Note this is a convenience gap, not a wire-correctness one: the answer is already kept rather than rejected, which is more than Python does.


## response-8. [low, behaviour-gap] CONFIRMED: Java accepts stringified numbers that both SDKs reject

- Java: `/Users/jim/src/typesafe-client/typesafe-client/src/main/java/software/sava/typesafe/Answer.java:77,87,88 (`ji.readDouble()`) and Usage.java:26-27 (`ji.readLong()`)`
- Reference: `/Users/jim/docs/ai/typesafe/typesafe-sdk-python/tests/test_responses.py:34 (`{"n": {"type": "noul", "noul": "0.5"}}` → field_path `answers.n.noul`) with the strict config at /Users/jim/docs/ai/typesafe/typesafe-sdk-python/src/typesafe_sdk/_core/response_types.py:28`
- Java does: jsoniter's readDouble accepts NUMBER or STRING (BaseJsonIterator.java:864-874) and readLong unwraps a quoted number (BaseJsonIterator.java:1381-1388), so `"noul":"0.5"` and `"input_tokens":"12"` parse to 0.5 and 12.
- Reference does: Python's answer models are `strict=True`, so a quoted number is a validation error with an exact field path; the existing Java test only covers the unparseable `"x"` case (AnswerParseTests.java:92).
- Consequence: Java is quietly more permissive than the reference clients on the same body. Harmless in itself, but it means a server-side regression that starts quoting numbers would be caught by the SDKs' contract tests and not by Java's, so the two clients would disagree about whether the API is broken.
- Refuter's evidence: Answer.java:77 `case 1 -> noul = ji.readDouble();`, :87 confidence, :88 score, and Usage.java:26-27 readLong. BaseJsonIterator.readAsDouble is `final var valueType = whatIsNext(); if (valueType == NUMBER) {…} else if (valueType == STRING) { nextToken(); return parse(parseChars); } else throw reportError(...)`, and readLong() opens with `final char c = nextToken(); if (c == '"') { final long val = readLong(); if (nextToken() != '"') throw reportError("readLong", "Lenient parsing of number string did not close with a quote."); return val; }` — so `"noul":"0.5"` parses to 0.5 and `"input_tokens":"12"` to 12. Python is strict: `model_config = ConfigDict(extra="ignore", frozen=True, strict=True)` on every answer model (_core/response_types.py:28, :39, :49), and tests/test_responses.py:34 pins `({"n": {"type": "noul", "noul": "0.5"}}, "answers.n.noul")` as a validation error. The existing Java coverage is only the unparseable case, AnswerParseTests.java:92 `assertThrows(RuntimeException.class, () -> answer("{\"type\":\"noul\",\"noul\":\"x\"}"))`. One wording nit: "both SDKs reject" holds for Python only — JS does no runtime validation at all (client.ts:342-346 `parsed as T`), so a quoted number reaches a JS caller as the string "0.5".
- **Corrected proposal:** Decide the leniency deliberately: either document that Java accepts quoted numbers where Python's strict models reject them (_core/response_types.py:28 with test_responses.py:34), or reject non-NUMBER tokens at Answer.java:77,87,88 and Usage.java:26-27. Either way add an AnswerParseTests case pinning the chosen behaviour for `{"type":"noul","noul":"0.5"}`, so a server-side regression that starts quoting numbers is not caught by Python's contract tests and missed by Java's. Reword the title to "that the Python SDK rejects": JS does no runtime validation (client.ts:342-346), so it would hand the caller the string rather than reject it.


## response-9. [low, behaviour-gap] CONFIRMED: GET /v1/models discards the request id and the raw body

- Java: `/Users/jim/src/typesafe-client/typesafe-client/src/main/java/software/sava/typesafe/TypeSafeClientImpl.java:36-43 and 96-98 (returns List<ModelCard>, nothing else); contrast SystemOneResponse.java:30-33 which does capture both`
- Reference: `/Users/jim/docs/ai/typesafe/typesafe-sdk-js/src/api-promise.ts:45-49 (`withResponse()` → data, response, requestId) with /Users/jim/docs/ai/typesafe/typesafe-sdk-js/test/client.test.ts:169-195; /Users/jim/docs/ai/typesafe/typesafe-sdk-python/src/typesafe_sdk/_core/schemas/base.py:80-94 and tests/test_responses.py:91-109`
- Java does: A successful models() call returns only the parsed cards. The `x-typesafe-request-id` header and the response body are dropped, and unmodelled per-card fields are unrecoverable.
- Reference does: Both SDKs expose the request id and the untouched body on every successful response, and JS has a dedicated test that an unmodelled `tags: ["internal"]` field on a model card survives via raw access.
- Consequence: A Java user cannot quote a request id when a models() call returns a surprising list, and cannot read employee-only or newly added card fields without re-issuing the call through the HttpClient directly.
- Refuter's evidence: TypeSafeClientImpl.java:36-43 — MODELS_PARSER is `Function<HttpResponse<?>, List<ModelCard>>` whose body is `gate(httpResponse)` then `ModelCard.parseList(JsonIterator.parse(body))`; the HttpResponse is used only for the status gate and the parse-failure wrap, never for its headers. models() at :96-97 returns `sendGetRequest(modelsEndpoint, MODELS_PARSER)`. Contrast SystemOneResponse.java:30-33, which does read `httpResponse.headers().firstValue(REQUEST_ID_HEADER)` and keeps `raw`. Unmodelled per-card fields are also unrecoverable: ModelCard's FieldMatcher covers only name/description/release_date and everything else hits `ji.skip()`. Both SDKs expose more: api-promise.ts:45-49 `withResponse()` returns `{ data, response, requestId: requestIdFrom(response.headers) }` and `asResponse()` at :41-43 hands back the untouched Response, with test/client.test.ts:187-195 asserting an unmodelled `tags: ["internal"]` survives via `asResponse()`; Python's _core/schemas/base.py:80-94 exposes `request_id` and `raw_http_response` on every Response, exercised at tests/test_responses.py:91-109.
- **Corrected proposal:** Return a small envelope from models() (cards + requestId + raw), mirroring SystemOneResponse.java:23-33, so a Java user can quote a request id for a surprising model list and read employee-only or newly added card fields without re-issuing the call through HttpClient directly. Requires threading the HttpResponse headers into MODELS_PARSER (TypeSafeClientImpl.java:36-43), which today uses the response only for the status gate. Test against the models body with the extra `tags` field from client.test.ts:187-195. Note this is an API-surface change to a public return type, so it belongs before the release rather than after.
