# Parity findings, lens: fixtures

Each entry: verdict from an independent refuter, then the finder's claim and the refuter's evidence. Treat the corrected proposal as the spec.


## fixtures-1. [high, fixture] CONFIRMED: Score legend values can be JSON objects or arrays; Java reads them as strings

- Java: `/Users/jim/src/typesafe-client/typesafe-client/src/main/java/software/sava/typesafe/Answer.java:89-96 (parser); test gap in /Users/jim/src/typesafe-client/typesafe-client/src/test/java/software/sava/typesafe/AnswerParseTests.java:52-77 — every legend fixture there is string-valued`
- Reference: `/Users/jim/docs/ai/typesafe/typesafe-sdk-python/src/typesafe_sdk/_schemas/models.py:121 (`legend: dict[str, str | dict[str, Any] | list[Any]]`, generated from https://api.typesafe.ai/openapi.json); response fixture at /Users/jim/docs/ai/typesafe/typesafe-sdk-python/tests/test_clients.py:215-224 and 243; nested-JSON legend at /Users/jim/docs/ai/typesafe/typesafe-sdk-python/tests/test_responses.py:178-215`
- Java does: `Answer.Parser` case 6 reads each legend value with `ji1.readString()` into a `LinkedHashMap<String,String>`, and `ScoreAnswer` declares `Map<Integer,String> legend`. A legend value that is an object or array cannot be represented.
- Reference does: The OpenAPI-generated schema types the legend as `str | dict | list`, because the legend echoes back the criteria that were sent. `test_rich_descriptions` sends `Score(criteria=[{"summary": "duplicated", "examples": ["charged twice"]}])` and asserts the response legend comes back as that same object: `result.scores["risk"].legend[0] == criteria` against body `{"risk":{"type":"score","score":0,"confidence":1,"legend":{"0":{"summary":"duplicated","examples":["charged twice"]}},"probabilities":{"0":1}}}`.
- Consequence: Any caller who uses rich (object or array) score criteria — which the Java client itself supports, since `Score.levels` is `List<JsonContent>` — gets a `TypeSafeParseException` on the whole response instead of an answer. The failure is silent until the first non-string rubric is used in production.
- Refuter's evidence: Answer.java:89-96 reads each legend value with `ji1.readString()` into `LinkedHashMap<String,String>`; ScoreAnswer legend is `Map<Integer,String>` (Answer.java:39, :54). json-iterator's readString (BaseJsonIterator.java:205-214, json-iterator 25.3.2 sources) throws `reportError("readString", "expected string or null, but {")` on an object or array, so the whole response fails; TypeSafeClientImpl.java:27-33 wraps that RuntimeException in TypeSafeParseException. The schema types the legend as `dict[str, str | dict[str, Any] | list[Any]]` (python models.py:121) and test_clients.py:221 returns `"legend":{"0":{"summary":"duplicated","examples":["charged twice"]}}` with test_clients.py:243 asserting `result.scores["risk"].legend[0] == criteria`. Every legend in AnswerParseTests.java:52-77 is string-valued. Java's own Score.levels is `List<JsonContent>` (Score.java:14), so it can send what it cannot read back.
- **Corrected proposal:** Add the rich-legend response body from test_clients.py:215-224 to `TestBodies` and a case in `AnswerParseTests` asserting the intended behaviour; that test fails today (it throws at Answer.java:92) and forces a decision on widening `ScoreAnswer.legend` to `Map<Integer,JsonContent>` (or at minimum capturing raw JSON text) before release. Note the narrower scope: a `null` legend value already works (readString returns null on 'n'), so only object- and array-valued levels break.


## fixtures-2. [high, fixture] CONFIRMED: Answers missing their payload field parse to silent zeros instead of failing

- Java: `/Users/jim/src/typesafe-client/typesafe-client/src/main/java/software/sava/typesafe/Answer.java:34-59 (primitive fields default to 0.0, `create()` only checks `type`); nearest test /Users/jim/src/typesafe-client/typesafe-client/src/test/java/software/sava/typesafe/AnswerParseTests.java:86-88 checks only a missing `type``
- Reference: `/Users/jim/docs/ai/typesafe/typesafe-sdk-python/tests/test_responses.py:29-58 (parametrised malformed-answer table) and the schema's required fields at /Users/jim/docs/ai/typesafe/typesafe-sdk-python/src/typesafe_sdk/_schemas/models.py:107-133 and 209-217`
- Java does: `{"type":"noul"}` yields `NoulAnswer(0.0)`; `{"type":"choice","choice":"a","probabilities":{}}` yields `ChoiceAnswer("a", {}, 0.0)`; `{"type":"choice","confidence":0.5,...}` yields `ChoiceAnswer(null, ...)`. Nothing throws.
- Reference does: The Python SDK raises `TypeSafeAPIResponseValidationError` with an exact field path for each of those bodies — `answers.n.noul`, `answers.c.confidence`, `answers.c.choice` — and asserts the 200 status and request id travel on the error.
- Consequence: A truncated or partially-serialized answer becomes a confident-looking `noul() == 0.0` or `confidence() == 0.0`. Callers thresholding on those values silently take the "definitely no" / "no confidence" branch on data the SDKs reject outright, and `choice()` can return null.
- Refuter's evidence: Answer.Parser declares primitive `double noul/confidence/score` (Answer.java:35-40) and `create()` (Answer.java:44-57) checks only `type`, so `{"type":"noul"}` yields `NoulAnswer(0.0)`, `{"type":"choice","choice":"a","probabilities":{}}` yields confidence 0.0, and `{"type":"choice","confidence":0.5,...}` yields `choice() == null`. AnswerParseTests.java:86-88 pins only the missing-`type` case. Python's parametrised table at tests/test_responses.py:29-40 raises `TypeSafeAPIResponseValidationError` with field paths `answers.n.noul`, `answers.c.confidence`, `answers.c.choice` and asserts status 200 and request id on the error (test_responses.py:51-57). Two citation nits that do not change the substance: the table is eight rows, not six, and the required-answer-field schema is models.py:11-40 (ChoiceAnswer) and :73-105 (NoulAnswer), not :209-217 (that block is SystemOneRequest.questions).
- **Corrected proposal:** Port the eight-row table from test_responses.py:29-40 into `AnswerParseTests` as a parametrised test, and decide per row whether Java should throw or document the zero default; at minimum pin a null `choice()` and a 0.0 `noul` so the behaviour is a deliberate, tested contract. Two rows also need a Java-specific decision: `legend:[]` and `legend:{"x":"bad"}` — the latter throws NumberFormatException today via `levelKeys` (Answer.java:60-69).


## fixtures-3. [medium, fixture] CONFIRMED: Both SDKs retry HTTP 408; Java's canBeRetried() says no, and no test covers 408

- Java: `/Users/jim/src/typesafe-client/typesafe-client/src/main/java/software/sava/typesafe/exceptions/TypeSafeRequestException.java:48-50 (`statusCode == 429 || statusCode >= 500`); the status tables at /Users/jim/src/typesafe-client/typesafe-client/src/test/java/software/sava/typesafe/exceptions/TypeSafeRequestExceptionTests.java:40-45 list neither 408 nor 502/504`
- Reference: `/Users/jim/docs/ai/typesafe/typesafe-sdk-python/src/typesafe_sdk/_core/retry.py:64 (`{408, 429, *range(500, 600)}`) and /Users/jim/docs/ai/typesafe/typesafe-sdk-js/src/retry.ts:16-17 (`/** HTTP 408, 429, and 5xx responses. */ httpStatuses: new Set([408, 429, ...range(500, 600)])`)`
- Java does: Reports 408 Request Timeout as terminal. The retryable/terminal tables in the test assert 429/500/503/529 retryable and 400/401/404/422/499/199 terminal; 408 falls in neither list, so nothing catches the divergence.
- Reference does: Both reference SDKs include 408 in their default retryable status set, alongside 429 and all of 5xx, and the JS suite exercises the set directly (test/reliability.test.ts:265-280).
- Consequence: Since the Java client deliberately leaves backoff to the caller, `canBeRetried()` is the whole retry contract it exports. A caller that follows it drops a 408 as permanent where both official SDKs would have retried and succeeded.
- Refuter's evidence: TypeSafeRequestException.java:48-50 is `statusCode == 429 || statusCode >= 500`, and its javadoc (:6-9) names only 401/422/429/529. TypeSafeRequestExceptionTests.java:40-45 lists 429/500/503/529 retryable and 400/401/404/422/499/199 terminal — 408 in neither. Python retry.py:64 `http_statuses: set[int] = ... {408, 429, *range(500, 600)}`; JS retry.ts:16-17 the same set. One reference citation is wrong: reliability.test.ts:265-280 exercises a custom `httpStatuses: new Set([409])`, not the default; the test that actually pins 408 is typesafe-sdk-js/test/retry.test.ts:39, `it.each([408, 429, 500, 502, 503, 504, 529, 599])("retries %i by default")`.
- **Corrected proposal:** Extend the `retryabilityFollowsTheDocumentedStatuses` tables with the exact default set both SDKs pin — 408, 429, 502, 504, 529, 599 retryable — citing retry.ts:16-17 and retry.test.ts:39, and either add 408 to `canBeRetried()` or record in the javadoc at TypeSafeRequestException.java:46-47 why Java diverges.


## fixtures-4. [medium, fixture] PARTLY: The 422 fixture is an invented body shape, not the one the API sends

- Java: `/Users/jim/src/typesafe-client/typesafe-client/src/test/java/software/sava/typesafe/TypeSafeClientWireTests.java:131 and 136 use `{"detail":"criteria must not be empty"}``
- Reference: `/Users/jim/docs/ai/typesafe/typesafe-sdk-python/src/typesafe_sdk/_schemas/models.py:163-190 (`ValidationError.loc/msg/type`, `HTTPValidationError.detail: list[ValidationError]`); recorded bodies at /Users/jim/docs/ai/typesafe/typesafe-sdk-js/test/errors.test.ts:67-83 and /Users/jim/docs/ai/typesafe/typesafe-sdk-python/tests/test_clients.py:328-330; live shape confirmed at /Users/jim/docs/ai/typesafe/typesafe-sdk-js/test/integration/api.integration.ts:129-138`
- Java does: Pins the 422 path against a body where `detail` is a plain string. That shape is not what the API emits, so the test proves only that some bytes survive round-trip.
- Reference does: The generated schema makes `detail` a list of `{loc, msg, type, input?, ctx?}`; the JS test pins `{"detail":[{"type":"list_type","loc":["body","questions","q","score","criteria"],"msg":"Input should be a valid list"},{...}]}` and the live integration test confirms a real 422 renders as `422 questions.q.score.criteria.0...`.
- Consequence: The Java client's one 422 test does not demonstrate that a real validation failure survives `body()` and the 512-byte message truncation intact — a genuine `detail` array is far longer than the stub and is the case truncation actually has to handle.
- Refuter's evidence: The Java citation holds: TypeSafeClientWireTests.java:131 and :136 use `{"detail":"criteria must not be empty"}`, and no Java fixture carries a `detail` array. But "not what the API emits" is refuted: a string-valued `detail` is a shape both SDKs pin as real — typesafe-sdk-js/test/errors.test.ts:62 `[{ detail: "fastapi style" }, "fastapi style"]`, python tests/test_clients.py:328 `({"detail": "detail"}, "detail")`, and python tests/test_clients.py:195 returns an actual `httpx2.Response(422, json={"detail": "Invalid question"})`. What is true: the schema's validation-error shape (models.py:163-190, `HTTPValidationError.detail: list[ValidationError]`) is untested in Java, the array body is pinned at errors.test.ts:67-84 and confirmed live at api.integration.ts:129-137 (`/^422 questions\.q\.score\.criteria\.0/`), and the 512-byte truncation at TypeSafeRequestException.java:36 is only ever exercised on `"x".repeat(517)` (TypeSafeClientWireTests.java:162-171).
- **Corrected proposal:** Keep the string-`detail` case — both SDKs treat it as a real API shape — and add the FastAPI-shaped body from errors.test.ts:67-84 to `TestBodies` as a second 422 case, asserting `exception.body()` equals it verbatim, plus one whose `detail` array exceeds `MESSAGE_BODY_LIMIT` so the truncation path runs on realistic bytes rather than on repeated 'x'.


## fixtures-5. [medium, fixture] CONFIRMED: No test pins how models() behaves on the SDKs' unrecognized-envelope table

- Java: `/Users/jim/src/typesafe-client/typesafe-client/src/main/java/software/sava/typesafe/ModelCard.java:15-28 (absent `models` silently yields `List.of()`); tests cover only `{...,"models":[...],...}`, `{"models":[]}` and `{}` at /Users/jim/src/typesafe-client/typesafe-client/src/test/java/software/sava/typesafe/AnswerParseTests.java:150-155 and 165`
- Reference: `/Users/jim/docs/ai/typesafe/typesafe-sdk-js/test/client.test.ts:176-185 (`[null, [], {models:{models:[]}}, {models:null}, {models:"bad"}, {ok:true}]`) and /Users/jim/docs/ai/typesafe/typesafe-sdk-python/tests/test_clients.py:266-269 (`[None, {}, {"models":"bad"}, {"models":[{"name":"x"}]}]`)`
- Java does: `parseList` returns an empty list whenever the `models` key is absent or unmatched, and `{"models":"bad"}` / `{"models":null}` / a top-level `null` or `[]` body go entirely untested. `TypeSafeClientWireTests.java:239-241` covers only the `"["` truncation case.
- Reference does: Both SDKs pin every one of those shapes as a hard failure — JS with `Unexpected response shape from GET /v1/models`, Python with `TypeSafeAPIResponseValidationError`, including the case `{"models":[{"name":"x"}]}` where a card is missing `description` and `release_date`.
- Consequence: A proxy or gateway that returns `{"ok":true}` or an HTML-ish `null` body on the models path makes `models()` succeed with an empty list, which a caller reads as "the account has no models" rather than "the call failed".
- Refuter's evidence: ModelCard.parseList (ModelCard.java:15-28) returns `List.of()` when `holder.models` is null. Verified against json-iterator 25.3.2: a top-level `null` hits BaseJsonIterator.java:623-625 (`c == 'n'` → skipNull → return), so `models()` returns an empty list; `{"ok":true}` skips and returns empty; `{"models":null}` also returns empty, because JsonIterator.java:208-213 documents "A JSON null value reads as an empty list"; `[]` throws at BaseJsonIterator.java:626 ("expected [,{}n]"); `{"models":[{"name":"x"}]}` yields `ModelCard("x", null, null)` (ModelCard.java:33). Java tests cover only the populated envelope, `{"models":[]}` and `{}` (AnswerParseTests.java:150-155, :165) plus the `"["` truncation at TypeSafeClientWireTests.java:239-241. JS pins all six shapes as `Unexpected response shape from GET /v1/models` (client.test.ts:176-185) and Python raises on `[None, {}, {"models":"bad"}, {"models":[{"name":"x"}]}]` (test_clients.py:266-269).
- **Corrected proposal:** Add a parametrised `modelsEnvelopeShapes` test in `AnswerParseTests` over the union of the two SDK tables, asserting for each row whether Java throws or returns empty — specifically pinning that `null`, `{}`, `{"ok":true}` and `{"models":null}` all return an empty list indistinguishable from "no models", that `[]` and `{"models":"bad"}` throw, and that `{"models":[{"name":"x"}]}` yields a `ModelCard` with two null fields.


## fixtures-6. [medium, fixture] CONFIRMED: Error-body edge cases: non-UTF-8 bytes, bare `null`, `[]` and `42` are unpinned

- Java: `/Users/jim/src/typesafe-client/typesafe-client/src/main/java/software/sava/typesafe/exceptions/TypeSafeRequestException.java:35-40; tests cover only null, empty, at-limit and over-limit bodies at /Users/jim/src/typesafe-client/typesafe-client/src/test/java/software/sava/typesafe/exceptions/TypeSafeRequestExceptionTests.java:18-36 and /Users/jim/src/typesafe-client/typesafe-client/src/test/java/software/sava/typesafe/TypeSafeClientWireTests.java:162-171`
- Reference: `/Users/jim/docs/ai/typesafe/typesafe-sdk-python/tests/test_errors.py:140-152 (`b""`, `b"null"`, `b"[]"`, `b"42"`, `b"not JSON: \xff"`, 201-byte plain and JSON bodies) and /Users/jim/docs/ai/typesafe/typesafe-sdk-js/test/errors.test.ts:104-133 (`text/html` body, null body, JSON with no content-type)`
- Java does: `new String(body, UTF_8)` is applied unconditionally, so a lone `0xFF` byte becomes U+FFFD inside `body()` and inside the exception message — untested, as are `null`, `[]` and `42` bodies.
- Reference does: Python pins each row's rendered message exactly, including `"400 not JSON: �"` for the invalid-UTF-8 body and the distinct 200-character truncation behaviours for a plain-text versus a JSON body. JS pins that a `text/html` 502 body is kept as text and that a missing content-type still parses as JSON.
- Consequence: `body()` is documented as "the full response body as UTF-8 text", but for a binary or mis-encoded gateway body it is lossy, and no test says so. A caller re-serializing `body()` into a log or a bug report writes replacement characters it cannot distinguish from real content.
- Refuter's evidence: TypeSafeRequestException.java:35-36 applies `new String(body, StandardCharsets.UTF_8)` unconditionally to both `body()` and the quoted message, and `body()`'s javadoc at :61 claims "the full response body as UTF-8 text". Java tests cover only null (TypeSafeRequestExceptionTests.java:18-25), at-limit/over-limit (:28-36), empty (TypeSafeClientWireTests.java:152) and the 517-byte 'x' body (:162-171). Python's table at tests/test_errors.py:140-152 pins `b""`, `b"null"`, `b"[]"`, `b"42"`, `b"not JSON: \xff"` (→ `"400 not JSON: �"`, line 147) and the distinct plain-vs-JSON truncations at :148-149; JS errors.test.ts:104-133 pins a text/html 502 kept as text, a null body as "429 status code (no body)", and JSON parsed without a content-type. One nuance worth stating: Python renders the same U+FFFD, so the lossy decode is parity, not a divergence — the defect is that it is undocumented and unpinned.
- **Corrected proposal:** Port the Python table at test_errors.py:140-152 into `TypeSafeRequestExceptionTests` as a parametrised test over raw `byte[]` bodies, asserting `body()` and `getMessage()` for each, including the `0xFF` row, so the lossy UTF-8 decode is a stated contract; note in the `body()` javadoc (TypeSafeRequestException.java:61) that a non-UTF-8 body comes back with replacement characters, as it does in the Python SDK.


## fixtures-7. [medium, fixture] PARTLY: The wire tests assert two request headers; both SDKs pin the full outgoing set

- Java: `/Users/jim/src/typesafe-client/typesafe-client/src/test/java/software/sava/typesafe/TypeSafeClientWireTests.java:98-100 (Authorization and Content-type only) and :230 (Authorization only on GET); the client sends nothing else, see /Users/jim/src/typesafe-client/typesafe-client/src/main/java/software/sava/typesafe/TypeSafeClientImpl.java:86-98`
- Reference: `/Users/jim/docs/ai/typesafe/typesafe-sdk-python/tests/test_clients.py:379-419 and 428-440; /Users/jim/docs/ai/typesafe/typesafe-sdk-js/test/client.test.ts:132-147 and /Users/jim/docs/ai/typesafe/typesafe-sdk-js/test/release-regressions.test.ts:46-70`
- Java does: Captures the whole request header map on the loopback server (`Wire.headers`) but asserts only `Authorization` and `Content-type`, and never asserts which headers are absent on the GET.
- Reference does: Both SDKs assert the complete set on every attempt: `authorization: Bearer <key>`, `accept: application/json`, `user-agent: typesafe-sdk/<version>`, `x-typesafe-sdk` equal to the user-agent, `x-typesafe-runtime` matching `node/x.y.z (…)` or `python/…`, `content-type: application/json` on POST, and explicitly `content-type` absent on GET.
- Consequence: The Java client sends no `Accept` and no SDK/runtime identification, so TypeSafe cannot attribute its traffic and a future content negotiation change would hit it first. Nothing in the suite would notice if `extendRequest` or the sava base class started or stopped sending a header.
- Refuter's evidence: The gap is real and worse than stated. Headers come from exactly two places: sava-rpc's `JsonHttpClient.newJsonRequest` (sava-rpc 25.9.1 sources, JsonHttpClient.java:85-90) sets only `Content-Type: application/json` and the timeout, and TypeSafeClient.java:132 adds `Authorization: Bearer <key>` — no Accept, no User-Agent beyond the JDK default, no SDK/runtime identifier. TypeSafeClientWireTests.java:98-100 asserts only Authorization and Content-type, and :230 only Authorization on the GET. But the proposal is wrong on one point: JsonHttpClient.java:85-90 sets Content-Type on *every* request, so Java does send `Content-Type: application/json` on the bodiless GET — the exact header both SDKs assert is absent (release-regressions.test.ts:60-70 `expect(headers.has("content-type")).toBe(false)`; client.test.ts:147 `expect(headers["Content-Type"]).toBeUndefined()`). A test written as proposed would fail for the wrong reason.
- **Corrected proposal:** Add a `theOutgoingHeaderSetIsPinned` test modelled on client.test.ts:132-147 that asserts the exact header map for both POST and GET from `Wire.headers`, recording today's truth: Authorization plus Content-type on both, and nothing else. Then decide before release on two separate items — whether to add `Accept: application/json` and an `X-TypeSafe-SDK`/runtime identifier, and whether to strip `Content-Type` from the GET, which sava-rpc's shared `newJsonRequest` sends unconditionally and which both SDKs deliberately omit.


## fixtures-8. [medium, fixture] PARTLY: The cross-SDK "duplicate charge" ticket request is the one fixture all three clients could share

- Java: `/Users/jim/src/typesafe-client/typesafe-client/src/test/java/software/sava/typesafe/TypeSafeLiveCheck.java:26-31 uses a different, Java-only state and question set; the serialization goldens in /Users/jim/src/typesafe-client/typesafe-client/src/test/java/software/sava/typesafe/QuestionSerializationTests.java:23-32 use the departments example`
- Reference: `/Users/jim/docs/ai/typesafe/typesafe-sdk-python/tests/test_integration.py:25-48 and /Users/jim/docs/ai/typesafe/typesafe-sdk-js/test/integration/api.integration.ts:28-85`
- Java does: Exercises a `String` state with three questions of its own invention; there is no request body the Java client and either SDK are known to produce identically.
- Reference does: Both SDKs run the *same* live case: state `{subject: "Charged twice this month", body: "I see two charges of $49…"}`, a noul (`Is this ticket about billing?`), a choice over `{calm, frustrated, angry}`, and a score over `["can wait", "this week", "today"]` — then assert the same invariants (probabilities sum ≈ 1, legend echoes the criteria by level, score in 0..2).
- Consequence: There is no evidence that the Java client's serializer produces a body byte-identical to what the reference SDKs send for the same logical request, which is exactly the check two reference clients make cheap.
- Refuter's evidence: The Java side holds: TypeSafeLiveCheck.java:26-31 uses a Java-only shoes/returns state with its own three questions, and QuestionSerializationTests.java:23-32 uses the departments example, so no request body is known-identical to an SDK's. But "Both SDKs run the *same* live case" is overstated: the state bodies differ (python tests/test_integration.py:27 "I see two charges of $49. I only have one account. Please fix this ASAP." vs js test/integration/api.integration.ts:29-30 "...two charges of $49 on my card for August. I only have one account..."), and Python's billing noul carries rich criteria (`{"true": {"meaning": ..., "examples": [...]}}`, test_integration.py:33) where JS sends `noul("Is this ticket about billing?")` bare (api.integration.ts:52). Byte-identity is also unreachable: Java writes `state,model,questions` (QuestionSerializationTests.java:30), while JS's serialized key order is never pinned (client.test.ts:336 uses `toEqual`, which is order-insensitive). The proposal's source citation is also mismatched — test_clients.py:77-85 is the `expected` for `test_round_trip`, state `{"document": "Hello 🌍"}`, not the ticket.
- **Corrected proposal:** Rebuild the shared ticket case in `QuestionSerializationTests` as a golden string and in `TypeSafeLiveCheck`, taking the request from python test_integration.py:25-38 and the invariants from api.integration.ts:63-85 (probabilities sum ≈ 1, legend echoes the criteria by level, score in 0..2). Pin it as semantically equal JSON, not as bytes shared with the SDKs: the two SDKs' own live bodies already differ in state text and noul criteria, and JS never pins its key order.


## fixtures-9. [medium, fixture] CONFIRMED: `"usage":{}` and partially-reported token counts are untested

- Java: `/Users/jim/src/typesafe-client/typesafe-client/src/main/java/software/sava/typesafe/Usage.java:20-31 (primitive `long` fields, absent means 0); the only usage tests are /Users/jim/src/typesafe-client/typesafe-client/src/test/java/software/sava/typesafe/AnswerParseTests.java:129-135 (usage absent → null) and :159`
- Reference: `/Users/jim/docs/ai/typesafe/typesafe-sdk-python/tests/test_types.py:66 (`{"model": "jev-latest", "usage": {}, "answers": {}}`), /Users/jim/docs/ai/typesafe/typesafe-sdk-python/src/typesafe_sdk/_core/response_types.py:65-73 (`input_tokens: int | None = None`, "or `None` when the API did not report it"), /Users/jim/docs/ai/typesafe/typesafe-sdk-python/tests/test_integration.py:40-41`
- Java does: Distinguishes "no usage object" (null `Usage`) from "usage present" but collapses a present-but-empty or partial usage object to `new Usage(0, 0)`, with no test covering it.
- Reference does: The Python SDK deliberately widens the generated schema's required `int` fields to `int | None` because the API may omit them, and its live test asserts `usage.input_tokens is None or > 0`. `{"usage": {}}` is used as a routine response body across its type tests.
- Consequence: A caller metering spend on `response.usage().inputTokens()` cannot tell "zero tokens billed" from "the API did not report it", and the Java live check at TypeSafeLiveCheck.java:45 asserts `inputTokens() > 0` — which the SDKs' own contract says may legitimately be unreported.
- Refuter's evidence: SystemOneResponse.java:106 calls `Usage.parse` whenever the key is present, and Usage's primitive `long` fields (Usage.java:20-21) leave an absent count at 0, so `{"usage":{}}` becomes `new Usage(0, 0)` — indistinguishable from zero billed. Java tests cover only usage absent → null (AnswerParseTests.java:129-135) and both counts present (:159). Python widens the generated required ints (models.py:151-160) to `input_tokens: int | None = None` with the comment "or `None` when the API did not report it" (response_types.py:65-73), uses `{"usage": {}}` as a routine body (test_types.py:66, :80, :97) and asserts `usage.input_tokens is None or > 0` live (test_integration.py:40). One correction to the consequence: the JS live test asserts `expect(data.usage.input_tokens).toBeGreaterThan(0)` (api.integration.ts:64), so TypeSafeLiveCheck.java:45 matches JS; only Python treats the counts as optional.
- **Corrected proposal:** Add cases to `AnswerParseTests` for `{"model":"m","usage":{}}` and `{"usage":{"input_tokens":619}}`, pinning the 0 default explicitly, and document in `Usage` (Usage.java:7) that 0 also means "not reported", citing response_types.py:70. Leave TypeSafeLiveCheck.java:45 as is unless the omission is observed live — JS asserts the same thing.


## fixtures-10. [low, fixture] PARTLY: Forward-compatible extra fields are pinned only for model cards, not answers or usage

- Java: `/Users/jim/src/typesafe-client/typesafe-client/src/test/java/software/sava/typesafe/AnswerParseTests.java:150-155 covers an unknown field inside a model card and :37 covers `"extra":[1,{"a":2}]` inside a choice answer; nothing covers unknown fields in `usage` or at the response top level (parser: /Users/jim/src/typesafe-client/typesafe-client/src/main/java/software/sava/typesafe/Usage.java:29)`
- Reference: `/Users/jim/docs/ai/typesafe/typesafe-sdk-python/tests/test_responses.py:140-154 (`usage` with `reasoning_tokens` and `billing_units`, a noul answer with `explanation`), /Users/jim/docs/ai/typesafe/typesafe-sdk-python/tests/test_clients.py:256-263 (`context_window`, `pricing: null`), /Users/jim/docs/ai/typesafe/typesafe-sdk-js/test/client.test.ts:187-195 (`tags: ["internal"]`)`
- Java does: Each parser's `default -> ji.skip()` should tolerate unknown fields everywhere, but only two of the four parsers have a test proving it.
- Reference does: Both SDKs pin forward compatibility explicitly at every level, including the named future fields `reasoning_tokens`, `billing_units`, `explanation`, `context_window`, `pricing` and employee-only `tags`, and assert the unknown data is still reachable through the raw response.
- Consequence: A future server-side field added inside `usage` or beside `answers` would only be caught by the fuzz corpus, not by a named regression test — and the Java client's equivalent escape hatch, `SystemOneResponse.raw()`, is never asserted to contain a field the parser dropped.
- Refuter's evidence: The `usage` half is refuted: AnswerParseTests.java:159 already parses `{"output_tokens":2,"x":null,"input_tokens":1}` and asserts `new Usage(1, 2)`, which is exactly the `default -> ji.skip()` at Usage.java:28 under test. So three of the four parsers are covered, not two (choice answer at :37, model card at :150-153, usage at :159). What stands: nothing covers an unknown field at the SystemOneResponse top level — `TestBodies.SMOKE` (TestBodies.java:10) has only model/answers/usage, and no other test parses a response with an extra top-level key (grep of the test sources shows SystemOneResponse.parse only at AnswerParseTests.java:94, :99, :130, RecordingTypeSafeClientTests.java:40 and the fuzzer) — and `raw()` is only ever compared to a body with nothing dropped (AnswerParseTests.java:102), so the escape hatch at SystemOneResponse.java:107 is never shown to preserve a skipped field.
- **Corrected proposal:** Add one `unknownFieldsAreSkippedEverywhere` test using the body from test_responses.py:141-145 (`reasoning_tokens`, `billing_units`, and an `explanation` on a noul answer) plus an unknown top-level key, asserting the parse succeeds and that `raw()` still contains `reasoning_tokens`. Drop the claim that `usage` is uncovered — AnswerParseTests.java:159 already pins it; the gaps are the response top level and the `raw()` escape hatch.


## fixtures-11. [low, fixture] CONFIRMED: Rich object score levels and array instructions are unpinned on the request side

- Java: `/Users/jim/src/typesafe-client/typesafe-client/src/test/java/software/sava/typesafe/QuestionSerializationTests.java:72-88 pins structured instructions and criteria for `Choice` only; every `Score` golden (:35-44, :186-192) uses plain strings, and `Question.score` at /Users/jim/src/typesafe-client/typesafe-client/src/main/java/software/sava/typesafe/Question.java:49-55 only accepts `String` levels`
- Reference: `/Users/jim/docs/ai/typesafe/typesafe-sdk-python/tests/test_clients.py:201-243 (`Score(instructions="Risk?", criteria=[{"summary": …, "examples": […]}])`), /Users/jim/docs/ai/typesafe/typesafe-sdk-js/test/client.test.ts:289-294 and 337-356 (`score(null, [null, "high"])`, `noul([null, {examples: [1, false]}], …)`), schema at /Users/jim/docs/ai/typesafe/typesafe-sdk-python/src/typesafe_sdk/_schemas/models.py:142-149`
- Java does: `Score.levels` is `List<JsonContent>` so the shape is expressible, but no golden asserts the bytes, and the public convenience factory cannot build one — a caller must construct `new Score(...)` directly.
- Reference does: The schema types score criteria as `list[str | dict | list]`; both SDKs test object-valued and null-valued levels and array-valued instructions as first-class wire input.
- Consequence: The serializer path for object score levels is untested, and it is the same path whose response counterpart (the echoed legend) is broken — so the two halves of rich-rubric support are unverified together.
- Refuter's evidence: `Question.score` accepts only `List<String>`/`String...` (Question.java:49-55), so the documented rich shape is reachable only through `new Score(...)` directly, even though `Score.levels` is `List<JsonContent>` (Score.java:14) and its javadoc says "string, object, array, or null" (Score.java:13). Every score golden uses plain strings (QuestionSerializationTests.java:35-44, :186-192); structured instructions and criteria are pinned for `Choice` only (:72-88). The schema types score criteria as `list[str | dict[str, Any] | list[Any]]` (models.py:142-149), Python sends `Score(instructions="Risk?", criteria=[{"summary": ..., "examples": [...]}])` (test_clients.py:209, :240), and JS pins `score(null, [null, "high"])` (client.test.ts:290) and array instructions `noul([null, {examples: [1, false]}], ...)` (client.test.ts:341).
- **Corrected proposal:** Add a `richScoreLevelsSerialize` golden mirroring test_clients.py:209, plus a `Question.score(String, List<JsonContent>)` overload (or a differently named factory, since `score(String, List<String>)` at Question.java:49 would be erasure-ambiguous) so the shape Score.java:13 documents is reachable without the record constructor.


## fixtures-12. [low, fixture] CONFIRMED: A 2xx with no body (204) is accepted by the status gate but never exercised

- Java: `/Users/jim/src/typesafe-client/typesafe-client/src/main/java/software/sava/typesafe/TypeSafeClientImpl.java:47-54 admits every 2xx; /Users/jim/src/typesafe-client/typesafe-client/src/test/java/software/sava/typesafe/TypeSafeClientWireTests.java:184-193 tests 299 with a full body and 300/404, but no empty-bodied success`
- Reference: `/Users/jim/docs/ai/typesafe/typesafe-sdk-js/test/release-regressions.test.ts:148-153 (`new Response(null, {status: 204})` → `asResponse()` returns status 204 with a null body and no stream read)`
- Java does: A 204 reaches `SystemOneResponse.parse` / `ModelCard.parseList` with a zero-length body, which surfaces as a `TypeSafeParseException` whose message quotes an empty string — behaviour nothing asserts.
- Reference does: The JS SDK has a named regression test that a null-bodied 2xx is returned without attempting to read a stream, added deliberately as a release regression.
- Consequence: A load balancer or a future endpoint returning 204 produces a confusing `Failed to adapt 204 response: ''` rather than a clear signal, and the parse-exception path for empty input is only reached via the fuzzer.
- Refuter's evidence: `gate` admits every 2xx (TypeSafeClientImpl.java:47-54) and hands the bytes straight to `SystemOneResponse.parse` / `ModelCard.parseList` (:27-43). On a zero-length body json-iterator throws at BytesJsonIterator.java:112-116 (`reportError("nextToken", "unexpected end")`), which parseFailure (TypeSafeClientImpl.java:56-64) renders as `Failed to adapt 204 response: ''`. Nothing asserts it: TypeSafeClientWireTests.java:184-193 tests 299 with a full body plus 300/404, and TypeSafeClientImplTests.java:16-27 pins the gate at 200/299 with `"{}"`. JS has a named regression that a null-bodied 204 is returned without reading a stream (release-regressions.test.ts:148-153).
- **Corrected proposal:** Add an `anEmptyBodiedSuccessIsAParseException` case pinning today's `Failed to adapt <status> response: ''` message for both paths. Drive it through `TypeSafeClientImpl.gate` plus `FakeHttpResponse` and an empty `byte[]`, the way TypeSafeClientImplTests.java:16-27 already pins the gate — `jdk.httpserver` cannot send a body with a 204, and the harness's `sendResponseHeaders(status, 0)` at TypeSafeClientWireTests.java:51 means chunked, not empty — or serve a 200 with `""` over the wire if an end-to-end case is wanted.


## fixtures-13. [low, fixture] CONFIRMED: One-level score criteria: JS rejects, Python and the schema allow — Java's choice is right but uncited

- Java: `/Users/jim/src/typesafe-client/typesafe-client/src/main/java/software/sava/typesafe/Score.java:20-22 (rejects empty only) and /Users/jim/src/typesafe-client/typesafe-client/src/test/java/software/sava/typesafe/QuestionSerializationTests.java:186-192 (`Question.score("q", "only")` is accepted, `topLevel() == 0`)`
- Reference: `/Users/jim/docs/ai/typesafe/typesafe-sdk-python/src/typesafe_sdk/_schemas/models.py:142-149 (`min_length=1`) and /Users/jim/docs/ai/typesafe/typesafe-sdk-python/tests/test_questions.py:169-174 (only `[]` rejected), versus /Users/jim/docs/ai/typesafe/typesafe-sdk-js/test/client.test.ts:377-395 (`'Score question "q" has 0 criteria; at least two scores are required.'`, and `["only"]` also rejected)`
- Java does: Accepts a single-level score, matching Python and the generated OpenAPI constraint `min_length=1`.
- Reference does: The JS SDK is stricter than the published schema and rejects a one-level score client-side; the Python SDK and the schema itself permit it.
- Consequence: A reviewer comparing Java against the JS SDK will read the Java behaviour as a missing validation. It is not — but nothing in the Java suite records why, so the next round of parity work may add a wrong restriction.
- Refuter's evidence: Score.java:18-24 rejects only an empty level list, and QuestionSerializationTests.java:186-192 accepts `Question.score("q", "only")` with `topLevel() == 0`. The published schema permits it: models.py:142-149 types ScoreQuestion.criteria with `min_length=1` (the constraint is at models.py:146), and Python rejects only `[]` (tests/test_questions.py:169-175, `test_empty_score_criteria_is_rejected`, matching '"rating" has no criteria'). JS is stricter than the schema: client.test.ts:388 `'Score question "q" has 0 criteria; at least two scores are required.'` and :391 `expect(() => bad(["only"])).toThrow("at least two scores")`.
- **Corrected proposal:** Keep the current behaviour and add a comment plus an explicit assertion in `scoreValidation` (QuestionSerializationTests.java:186-192) citing models.py:146 (`min_length=1`) and test_questions.py:169-175, so the divergence from client.test.ts:388-391 is a recorded decision rather than an omission.
